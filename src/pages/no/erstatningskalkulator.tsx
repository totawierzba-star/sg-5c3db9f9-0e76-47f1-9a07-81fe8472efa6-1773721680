import { LayoutNo } from "@/components/LayoutNo";
import { LocalizedCalculatorPage } from "@/components/LocalizedCalculatorPage";

export default function NoCompensationCalculator() {
  return (
    <LayoutNo>
      <LocalizedCalculatorPage locale="no" />
    </LayoutNo>
  );
}
