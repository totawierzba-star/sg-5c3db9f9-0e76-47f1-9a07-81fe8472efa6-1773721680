import { LayoutLt } from "@/components/LayoutLt";
import { LocalizedCalculatorPage } from "@/components/LocalizedCalculatorPage";

export default function LithuanianCompensationCalculator() {
  return (
    <LayoutLt>
      <LocalizedCalculatorPage locale="lt" />
    </LayoutLt>
  );
}
