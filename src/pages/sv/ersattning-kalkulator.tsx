import { LayoutSv } from "@/components/LayoutSv";
import { LocalizedCalculatorPage } from "@/components/LocalizedCalculatorPage";

export default function SvCompensationCalculator() {
  return (
    <LayoutSv>
      <LocalizedCalculatorPage locale="sv" />
    </LayoutSv>
  );
}
