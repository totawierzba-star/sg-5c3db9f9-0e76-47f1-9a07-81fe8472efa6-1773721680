import { LayoutBg } from "@/components/LayoutBg";
import { LocalizedCalculatorPage } from "@/components/LocalizedCalculatorPage";

export default function BulgarianCompensationCalculator() {
  return (
    <LayoutBg>
      <LocalizedCalculatorPage locale="bg" />
    </LayoutBg>
  );
}
