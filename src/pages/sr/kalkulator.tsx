import { LayoutSr } from "@/components/LayoutSr";
import { LocalizedCalculatorPage } from "@/components/LocalizedCalculatorPage";

export default function SerbianCompensationCalculator() {
  return (
    <LayoutSr>
      <LocalizedCalculatorPage locale="sr" />
    </LayoutSr>
  );
}
