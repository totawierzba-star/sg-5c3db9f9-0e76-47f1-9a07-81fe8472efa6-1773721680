import { LayoutEl } from "@/components/LayoutEl";
import { LocalizedCalculatorPage } from "@/components/LocalizedCalculatorPage";

export default function ElCompensationCalculator() {
  return (
    <LayoutEl>
      <LocalizedCalculatorPage locale="el" />
    </LayoutEl>
  );
}
