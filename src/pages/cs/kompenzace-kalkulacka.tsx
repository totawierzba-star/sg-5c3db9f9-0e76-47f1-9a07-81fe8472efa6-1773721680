import { LayoutCs } from "@/components/LayoutCs";
import { LocalizedCalculatorPage } from "@/components/LocalizedCalculatorPage";

export default function CsCompensationCalculator() {
  return (
    <LayoutCs>
      <LocalizedCalculatorPage locale="cs" />
    </LayoutCs>
  );
}
