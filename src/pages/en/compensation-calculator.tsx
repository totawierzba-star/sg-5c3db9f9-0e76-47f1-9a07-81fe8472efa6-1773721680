import { LayoutEn } from "@/components/LayoutEn";
import { LocalizedCalculatorPage } from "@/components/LocalizedCalculatorPage";

export default function CompensationCalculatorEn() {
  return (
    <LayoutEn>
      <LocalizedCalculatorPage locale="en" />
    </LayoutEn>
  );
}
