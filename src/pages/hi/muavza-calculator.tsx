import { LayoutHi } from "@/components/LayoutHi";
import { LocalizedCalculatorPage } from "@/components/LocalizedCalculatorPage";

export default function HiCompensationCalculator() {
  return (
    <LayoutHi>
      <LocalizedCalculatorPage locale="hi" />
    </LayoutHi>
  );
}
