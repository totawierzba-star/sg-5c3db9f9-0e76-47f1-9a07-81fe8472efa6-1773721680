import { LayoutTr } from "@/components/LayoutTr";
import { LocalizedCalculatorPage } from "@/components/LocalizedCalculatorPage";

export default function TrCompensationCalculator() {
  return (
    <LayoutTr>
      <LocalizedCalculatorPage locale="tr" />
    </LayoutTr>
  );
}
